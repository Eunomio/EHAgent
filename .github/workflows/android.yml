name: Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"
      - name: Compile APK
        run: gradle :app:assembleDebug --stacktrace 2>&1 | tee android-build-output.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-build-output
          path: android/android-build-output.txt
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: EHAgent-resident-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
